{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for FlowPOS Launcher",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:default"
  ]
}
